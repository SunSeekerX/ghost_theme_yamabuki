<div class="pagination-area">
  <div class="container">
    <div class="row">
      <div class="col-md-12 mx-auto">
        <div class="post-pagination clearfix">
          <div class="row">
            {{#if prev}}
              <div class="col">
                <p><a href="{{page_url prev}}">Prev</a></p>
              </div>
            {{else}}
              <div class="col">
              </div>
            {{/if}}

            <div class="col" style="text-align: center;">
              <p class="mb-0">Page {{page}} / {{pages}}</p>
            </div>
            {{#if next}}
              <div class="col" style="text-align: right;">
                <p class="mb-0"><a href="{{page_url next}}">Next</a></p>
              </div>
            {{else}}
              <div class="col">
              </div>
            {{/if}}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>